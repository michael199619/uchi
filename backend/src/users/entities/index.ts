import { User, UserStatus } from './user.entity';
import { Role } from './role.entity';
import { Chat } from './chat.entity';

export const UsersEntities = [User, Role, Chat];
export { User, Role, UserStatus, Chat };
